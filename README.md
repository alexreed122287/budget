# Budget

Personal budgeting tool — paycheck-based cashflow grid, recurring bills, mortgage payoff tracker, savings calculator, and net worth history.

Data lives in a private Google Sheet via a small Apps Script webhook (see `apps-script.gs` for one-time setup). The HTML template here contains no data.

Live: https://alexreed122287.github.io/budget/
